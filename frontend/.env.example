# ==============================================================================
# NEXUS CHECK FRONTEND - ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env.local for local development
# For production deployment on Vercel, set these in the Vercel dashboard
# ==============================================================================

# ------------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Public - Safe for Client-Side)
# ------------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Settings → API
# These are public keys protected by Row Level Security (RLS)

NEXT_PUBLIC_SUPABASE_URL=https://[your-project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ------------------------------------------------------------------------------
# BACKEND API URL
# ------------------------------------------------------------------------------
# Development: Point to local FastAPI server
# Production: Point to your Railway backend URL

# Development (uncomment for local dev):
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (uncomment and update for deployment):
# NEXT_PUBLIC_API_URL=https://[your-railway-app].railway.app

# ------------------------------------------------------------------------------
# ENVIRONMENT (Optional - Auto-set by Next.js)
# ------------------------------------------------------------------------------
# Next.js automatically sets NODE_ENV based on the build command
# - 'next dev' sets it to 'development'
# - 'next build' sets it to 'production'
# You typically don't need to set this manually

# NODE_ENV=development
